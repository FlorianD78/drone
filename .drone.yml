kind: pipeline
type: docker
name: default

steps:
  - name: test
    image: alpine
    commands:
      - echo le test est reussi

  - name: deploy
    image: alpine
    commands:
      - echo deploiement en cours
      - echo deploiement fin
  